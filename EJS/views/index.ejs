<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <p>Hello from EJS</p>
    <p>Day: <%= (day === 0 || day === 6)? "Weekend" : "Weekday" %></p>

    <% for(let fruit of fruits){ %>
    <li><%= fruit %></li>
    <% } %>
  </body>
</html>
